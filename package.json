{
  "name": "host-portal",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0 --port 5176 --strictPort",
    "test": "node --test tests/*.test.js",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4176",
    "export:teaser": "npm run build && node scripts/export-teaser.mjs",
    "deploy:preview": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "vite": "^8.3.0",
    "wrangler": "^4.135.0"
  }
}
